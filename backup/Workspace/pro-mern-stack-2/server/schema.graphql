type Issue {
    id: Int!
    title: String!
    status: String!
    owner: String
    effort: String
    created: String!
    due: String
}

type Query {
    about: String!
    issueList: [Issue!]!
}

type Mutation {
    setAboutMessage(message: String!): String
}