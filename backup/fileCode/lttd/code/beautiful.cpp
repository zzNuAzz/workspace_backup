#include <bits/stdc++.h>
using namespace std;
#define ll long long

void beauty(int n, int k) {
    if(n == 0) {
        cout << "NO";
        return;
    }
    ll min = pow(10, n-1);
    
}

int main() {
    int n, k;
    cin >> n >> k;
    beauty(n, k);
    return 0;
}

